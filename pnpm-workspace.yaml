packages:
  - "packages/*"
  - "apps/*"

# pnpm blocks dependency lifecycle scripts by default — allowlist only what must build.
allowBuilds:
  "@firebase/util": true
  protobufjs: true
minimumReleaseAgeExclude:
  - '@timonwa/app-utilities@0.4.0'
